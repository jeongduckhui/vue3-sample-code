# p6spy 활성화
decorator.datasource.p6spy.enable-logging=true

# slf4j
decorator.datasource.p6spy.logging=slf4j

# multiline
decorator.datasource.p6spy.multiline=true

# 핵심 설정
decorator.datasource.p6spy.log-message-format=com.p6spy.engine.spy.appender.CustomLineFormat

# 파라미터 바인딩 출력
decorator.datasource.p6spy.custom-log-message-format=\
%(currentTime)|%(executionTime)ms|%(category)|%(sql)

===================
spring.jpa.show-sql=false
logging.level.p6spy=DEBUG 또는 logging.level.com.p6spy=DEBUG




package com.example.demo.jasypt;

import org.jasypt.encryption.pbe.StandardPBEStringEncryptor;

public class JasyptDecryptor {

    public static void main(String[] args) {
        StandardPBEStringEncryptor encryptor = new StandardPBEStringEncryptor();

        encryptor.setPassword("네_암호키"); // 핵심

        String decrypted = encryptor.decrypt("ENC값 안쪽 문자열");

        System.out.println("복호화 결과: " + decrypted);
    }
}
