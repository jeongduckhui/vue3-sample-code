function buildGroupMap(columns) {
  const result = {};
  let currentMM = null;

  columns.forEach(({ code }) => {
    if (code.startsWith('MM')) {
      // 🔹 새 부모 그룹 생성
      currentMM = code;
      result[currentMM] = [];
    }

    if (code.startsWith('WW') && currentMM) {
      // 🔹 직전 MM 그룹에 추가
      result[currentMM].push(code);
    }
  });

  return result;
}
